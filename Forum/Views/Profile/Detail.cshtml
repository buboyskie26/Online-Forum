@model Forum.ViewModels.Profile.ProfileIndex;
@{
    ViewData["Title"] = "Detail";
}


<div class="container body-content">
    <div class="row sectionHeader">

        <div class="sectionHeading">
            User Profile
        </div>
    </div>

    <div class="row userProfile">
        <div class="col-md-4">
            <div class="col-md-1">
                <div class="profileAuthorImage" style="background-image: url(@Model.AuthorImageUrl); background-size:100%;"></div>
            </div>

            @if (Context.User.Identity.Name == Model.AuthorName)
            {
        <form method="post" asp-controller="Profile" asp-action="Detail" enctype="multipart/form-data">
            @*  <div id="upload">

            <label asp-for="ProfileImage" class="btn btn-dark btn-file">
                 Browse
            </label>
            <input asp-for="ProfileImage" type="file" value="Upload" />
            <span asp-validation-for="ProfileImage" class="text-danger"></span>
        </div>*@
            <div class="form-group col-md-1">
                <label asp-for="ProfileImage">Profile</label>
            </div>
            <div class="form-group col-md-4">
                <div class="btn btn-primary btn-icon-text">
                    <i class="fas fa-cloud-upload-alt"></i>
                    <input asp-for="ProfileImage" type="file" value="Upload" />
                    <span asp-validation-for="ProfileImage" class="text-danger"></span>
                </div>
            </div>
            <button type="submit" class="btn btn-outline-success btn-submit">Submit</button>
            <input asp-for="AuthorId" type="hidden" />
        </form>

            }

        </div>

            <div class="col-md-8">
                <div>
                    <span id="userName">@Model.AuthorName</span>

                    <span id="userRating">Current Rating: @Model.AuthorRating</span>
                    <span id="userEmailLabel">Email: @Model.AuthorEmail</span>
                    <span id="userCreatedLabel">Member Since: @Model.MemberSince</span>
                    <span id="userActiveLabel">
                        Status:
                        <span>Active</span>

                    </span>
                </div>
              

            </div>

    </div>
</div>
